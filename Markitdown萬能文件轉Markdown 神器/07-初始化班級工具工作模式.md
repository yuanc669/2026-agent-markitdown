# Claude Code 懶人包 #07：初始化班級工具工作模式

> 版本：v0.1
> 更新日期：2026-04-19
> 對應影片：Claude基本功 EP10「老師建專案指南」

> 📌 **本懶人包是「工作模式啟動包」**：跑完之後，你的 Claude Code 會進入「老師建專案模式」——以後加新工具、收工同步、跨 session 接續，都自動化。
>
> 一台電腦跑一次。換電腦時重跑（5 分鐘）即可。

---

## 這個懶人包會幫你做什麼？

### 一次性建立（5-10 分鐘）

- 「**班級工具總資料夾**」（在 Google 雲端硬碟）
- **CLAUDE.md** 專案藍圖（含 `.gitignore` 防 token 洩漏）
- **GitHub 公開 repo** + Pages 啟用
- **Obsidian 對應資料夾** + 工作筆記.md（駕駛艙）
- **`/收工` skill**（複製到 `~/.claude-skills/shutdown/`）
- **SessionEnd hook**（選配）— 忘記說「收工」的安全網

### 之後每次工作的節奏

| 場景 | 你說的話 | Claude 做什麼 |
|------|---------|---------------|
| 開新工具 | 「我想做一個 XXX 工具」 | 建子資料夾、引導跟著影片做 |
| **結束工作** | **「收工」** | **三方自動同步**：commit + push + 更新 Obsidian 工作筆記 |
| 接續工作 | 「讀工作筆記、告訴我上次做到哪」 | 摘要進度、建議下一步 |
| 老師查資料 | 「查 game_scores 集合最高分」 | 用 Firebase MCP 直接查 |

### 跨電腦怎麼辦？

| 同步層 | 換電腦時 |
|---|---|
| 工作檔案（GDrive） | ✅ 自動同步，不用做事 |
| GitHub repo | ✅ `git pull` 拿最新 |
| Obsidian 筆記 | ✅ 你的 vault 同步機制 |
| **Claude Code 設定（含 /收工 skill）** | **重跑這份懶人包**（5 分鐘） |

> 💡 進階：常在 2-3 台電腦切換的老師，看 EP14 學 chezmoi 自動同步（一般老師不用）。

---

## 先備條件

- [ ] Claude Code 桌面版（Pro 方案以上）已安裝、已登入
- [ ] **GitHub 已連好**（看過 EP05 + 跑過 [#02 連接 GitHub](02-連接-GitHub.md)）
- [ ] **Obsidian 已裝好**（看過 EP07 + 跑過 [#03 建立第二大腦](03-建立第二大腦-Obsidian.md)）
- [ ] **Firebase 已連好**（看過 EP09.5 + 跑過 [#04.5 連接 Firebase](04.5-連接-Firebase-資料庫.md)）
- [ ] 你知道 Obsidian vault 的路徑（例：`C:\Users\xxx\Documents\my-vault\`）

> 💡 還沒裝齊？依序看：EP05（#02）→ EP07（#03）→ EP09.5（#04.5）。這份懶人包是把它們**整合起來**用，不是替代。

---

## 請 Claude Code 幫我執行以下步驟

> ⚠️ 以下內容是**給 Claude Code 讀的操作指令**。
> 你只需要把這整份 MD 檔丟給 Claude Code 桌面版，它會自動開始執行。
> 遇到 🖐️ 標記就是需要你手動操作，Claude 會暫停告訴你怎麼做。

---

## 階段一：環境檢查

### 步驟 0：自動確認環境

請 Claude 執行以下檢查：

1. **作業系統**：執行系統指令確認是 Windows / macOS / Linux
2. **Git 已安裝**：`git --version`
3. **GitHub CLI 已登入**：`gh auth status`
   - 如果未登入：請使用者跑 `gh auth login`
4. **Node.js 已安裝**：`node --version`
5. **🖐️ 詢問使用者 Obsidian vault 路徑**（記下來、後面會用）
   - Windows 預設可能在 `C:\Users\<你的帳號>\Documents\<vault名>\`
   - macOS 預設可能在 `~/Documents/<vault名>/`
6. **驗證 vault 路徑存在**：`ls "<使用者給的路徑>"`

> 全部通過後，告知使用者環境 OK，繼續下一步。

---

## 階段二：建立工作環境

### 步驟 1：建立班級工具總資料夾

🖐️ **詢問使用者**：
- 「總資料夾要叫什麼名字？」（建議：英文小寫加連字號，例：`my-classroom-tools`、`<姓名>-tools`、`teacher-toolkit`）
- 「總資料夾要放在 Google 雲端硬碟哪裡？」（預設：`G:\我的雲端硬碟\` 底下）

```bash
mkdir -p "G:/我的雲端硬碟/<總資料夾名>"
cd "G:/我的雲端硬碟/<總資料夾名>"
```

> 💡 沒有 Google 雲端硬碟也行，OneDrive / Dropbox 任何雲端同步資料夾都可以。

---

### 步驟 2：建立 CLAUDE.md（專案藍圖）+ .gitignore

在總資料夾內建立 `CLAUDE.md`：

```markdown
# <總資料夾名> — 我的班級工具總專案

## 對話開始時請先讀
進度與最近更動都在 Obsidian：`<vault名>/<總資料夾名>/工作筆記.md`

## 工作模式
- **加新工具**：對 Claude 說「我想做一個 XXX 工具」→ Claude 會建 `tools/<工具名>/` 子資料夾、引導我跟著 EP10 影片做
- **結束工作**：對 Claude 說「**收工**」→ 自動 commit + push + 更新 Obsidian 工作筆記
- **接續工作**：對 Claude 說「讀工作筆記、告訴我上次做到哪」

## 工作桌 + 三個家
- 📋 GDrive 工作桌：`G:\我的雲端硬碟\<總資料夾名>\`（自動跨電腦同步）
- 🐙 GitHub repo：`<你的帳號>/<repo名>`（公開，網頁的家）
- 📘 Obsidian 駕駛艙：`<vault名>/<總資料夾名>/工作筆記.md`（想法的家）
- 🔥 Firebase 專案：`my-teaching-tools`（或你建的，資料的家）

## 工具清單
（之後加新工具時會自動更新）
- （尚無）

## 工作注意事項
- 學生資料一律去識別化（只用座號 + 班級代號）
- commit 訊息要寫清楚做了什麼 + 為什麼
- 收工前說「收工」讓 Claude 同步三方
```

並建立 `.gitignore`（**`.claude/` 一定要排除**）：

```
# Google Drive 系統檔
desktop.ini
*.tmp
~$*

# 敏感資料
.env
*.key
credentials.*

# Claude Code 本地設定（含 permissions、可能記錄 token，絕不上雲）
.claude/

# Node.js
node_modules/
package-lock.json
```

> 🔥 **`.claude/` 一定要排除**：Claude Code 會把你允許過的命令字串連 token 一起存進 `.claude/settings.local.json`，不排除可能洩漏 Firebase / Supabase token 到公開 repo。這條沒得商量。

---

### 步驟 3：git init + 建 GitHub 公開 repo + push

```bash
cd "G:/我的雲端硬碟/<總資料夾名>"
git config windows.appendAtomically false
git init
git add .
git commit -m "初始化班級工具總專案"
gh repo create <repo名> --public --source=. --push
```

> 🟡 **必須是公開 repo**——GitHub Pages 免費版只支援公開 repo。
> 🟡 GDrive 內 git 必須設 `windows.appendAtomically false`，否則 commit 會撞 Google Drive 同步。

push 完成後拿到 repo URL：`https://github.com/<你的帳號>/<repo名>`

---

### 步驟 4：在 Obsidian vault 建對應資料夾 + 工作筆記

```bash
mkdir -p "<vault路徑>/<總資料夾名>"
```

建立 `<vault路徑>/<總資料夾名>/工作筆記.md`：

```markdown
# <總資料夾名> 工作筆記

> 📌 進度日誌（變動快）。專案藍圖請看 GDrive 端的 `CLAUDE.md`。
> 進度只在這裡記錄，避免雙寫漂移。

## ⏯️ 上次做到哪

**最後動作**：（剛建好總專案、還沒做工具）
**所在 repo**：[<repo名>](https://github.com/<你的帳號>/<repo名>)

## 🛠️ 工具清單

（尚無，待加）

## 🗓️ 最近更動紀錄

| 日期 | 變更摘要 | GDrive | Obsidian | GitHub |
|------|----------|--------|----------|--------|
| <今天日期> | 初始化班級工具總專案 | ✅ | ✅ | ✅ |

## 🕳️ 踩坑筆記

（之後遇到坑就記在這）
```

---

## 階段三：安裝 /收工 skill

### 步驟 5：建立 SKILL.md

```bash
mkdir -p ~/.claude-skills/shutdown
```

在 `~/.claude-skills/shutdown/SKILL.md` 建立以下內容：

````markdown
---
name: shutdown
description: 收工同步助手。當使用者說「收工」、「結束了」、「準備換電腦」、「該同步的同步」、「先到這裡」等任何要結束工作並進行三方同步的請求時，請一定要使用此技能。本技能會智能更新 Obsidian 工作筆記、git commit + push GitHub。
---

# 收工同步助手

對話結束前，把今天的工作完整保存到三個家：
- **GDrive**：自動同步（不用管）
- **Obsidian 工作筆記**：智能更新「上次做到哪」+「最近更動紀錄」
- **GitHub**：commit + push 本 repo 變動

## 收工 SOP（依序執行）

### 步驟 1：盤點今天做了什麼
從對話歷史摘要：完成的檔案、決策、踩到的新坑。

### 步驟 2：找到工作目錄與工作筆記
- 當前 GDrive 工作目錄：`$PWD`（或從對話脈絡推斷）
- Obsidian 工作筆記：`<vault>/<同名資料夾>/工作筆記.md`
- 若 vault 沒對應資料夾 → 提醒使用者，但仍進行 GitHub 同步

### 步驟 3：更新 Obsidian 工作筆記
- 「⏯️ 上次做到哪」段：最後動作、完成的檔案、對話脈絡
- 「🗓️ 最近更動紀錄」表格最後加一行：今天日期 + 摘要 + ✅✅✅
- 「🕳️ 踩坑筆記」（若有新坑）：依分類加進去

### 步驟 4：Git commit + push
```bash
cd "<工作目錄>"
git config windows.appendAtomically false
git add <今天動到的檔案，不要 add .claude/>
git commit -m "<今天工作摘要的 commit message>"
git push origin <branch>
```

commit message 寫法：
- 標題行：「動詞 + 對象」
- 正文：3-5 條 bullet 描述變動 + 為什麼

### 步驟 5：報告同步狀態
給使用者一個三勾表格：

| 平台 | 動到的檔案 | 狀態 |
|------|----------|------|
| GDrive | ... | ✅（自動） |
| Obsidian | 工作筆記更新 | ✅ |
| GitHub | commit + push | ✅ |

## 不該做的事
- ❌ 對「沒實質進度」的對話也跑同步（例：使用者只是問問題沒改檔）
- ❌ 把 `.claude/settings.local.json`、`.claude/worktrees/` commit 進去
- ❌ commit message 寫「更新」、「修改」這種沒資訊的字
````

### 步驟 5.1：重啟 Claude Code 讓 skill 載入

🖐️ **手動操作**：完全關閉 Claude Code 桌面版，再重新開啟。

驗證：對 Claude 說「收工」，看是否自動觸發 SOP。如果觸發，✅ 完成。

---

### 步驟 5.5（選配）：要不要裝「忘記打收工的安全網」？

🖐️ **詢問使用者**：「要不要也裝 SessionEnd hook？這是『你忘記說收工、直接關 Claude Code 時，會自動做 git commit 備份』的安全網。」

- **不要**：跳到階段四
- **要**：繼續

#### 5.5.1 建立 session-cleanup.sh

```bash
mkdir -p ~/.claude/scripts
```

在 `~/.claude/scripts/session-cleanup.sh` 建立：

```bash
#!/bin/bash
# SessionEnd 安全網：對話結束時若 GDrive 內 repo 還有未提交變更，自動 commit + push

LOG_FILE="$HOME/.claude/scripts/session-cleanup.log"
log() { echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1" >> "$LOG_FILE"; }

INPUT=$(cat 2>/dev/null || echo "")
WORKDIR=$(echo "$INPUT" | python -c "import sys, json; d=json.load(sys.stdin); print(d.get('cwd', ''))" 2>/dev/null)
[ -z "$WORKDIR" ] && WORKDIR="$PWD"

log "========== SessionEnd 觸發 =========="
log "工作目錄：$WORKDIR"

# 只處理 GDrive 內 repo
case "$WORKDIR" in
    *雲端硬碟*) ;;
    *"My Drive"*) ;;
    *) log "  → 非 GDrive 目錄，跳過"; exit 0 ;;
esac

cd "$WORKDIR" || exit 0
[ -d ".git" ] || exit 0

git config windows.appendAtomically false 2>/dev/null
git add -u 2>/dev/null

if git diff --cached --quiet; then
    log "  → 無 modified tracked 檔案，跳過"
    exit 0
fi

REPO_NAME=$(basename "$WORKDIR")
git commit -m "[SessionEnd 自動保存] $(date +'%Y-%m-%d %H:%M')

對話結束時 SessionEnd hook 自動保存。詳細工作摘要請查 Obsidian 工作筆記：$REPO_NAME/工作筆記.md" >/dev/null 2>&1

git push origin HEAD >/dev/null 2>&1
log "  ✅ 已 commit + push"
```

設可執行：

```bash
chmod +x ~/.claude/scripts/session-cleanup.sh
```

#### 5.5.2 編輯 settings.json 加 hook

修改 `~/.claude/settings.json`，加入 `hooks` 段（若已有 hooks 段，把 SessionEnd 加進去）：

```json
{
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ~/.claude/scripts/session-cleanup.sh"
          }
        ]
      }
    ]
  }
}
```

> 💡 兩者**不衝突**：你正常打「收工」→ skill 接手；忘記打 → hook 接手做純備份。

---

## 階段四：跟著影片做第一個工具

### 步驟 6：建子資料夾

跟使用者說：「**現在來跟著 EP10 影片做第一個工具——座標獵人（純前端範式 A）**。我先幫你建子資料夾。」

```bash
cd "G:/我的雲端硬碟/<總資料夾名>"
mkdir -p tools/coordinate-hunter
cd tools/coordinate-hunter
```

---

### 步驟 7：複製句型範本給使用者

把以下句型秀給使用者，請他複製到 Claude Code（**這個對話就可以**）：

````
請做一個直角座標練習遊戲，叫「座標獵人」。
功能：
- 11×11 整數格點，xy 從 -5 到 5
- 隨機產生 10 個隱藏座標
- 學生輸入猜測（例：「2,-3」），命中就在棋盤上標記、計分 +1
- 60 秒倒數計時
- 結束顯示得分（滿分 10 分）
- 重新開始按鈕
風格：黑底白字、橘色強調，像粉筆寫黑板。
完成後預覽給我看。
````

完成後預覽 → 沒問題就 push：

```
都好，請 push 到 GitHub 並開啟 Pages，告訴我網址。
```

---

### 步驟 8：驗證收工流程

最後跟使用者說：

> 「現在試試看：對 Claude 說『**收工**』，看 skill 有沒有觸發。」

如果觸發成功：
- ✅ Skill 自動 commit + push + 更新工作筆記
- ✅ 你的「老師建專案工作模式」設定完成
- ✅ 之後每次都用「**我想做 XXX**」→ 做 → 「**收工**」的節奏

如果沒觸發：
- 確認你**重啟 Claude Code** 一次
- 確認 `~/.claude-skills/shutdown/SKILL.md` 存在
- 對 Claude 直接說「請按 ~/.claude-skills/shutdown/SKILL.md 的 SOP 幫我收工」就能手動執行

---

## 完成！接下來你的工作節奏

| 你說的話 | Claude 會做的事 |
|---------|----------------|
| 「我想做一個 XXX 工具」 | 建 `tools/xxx/` 子資料夾、引導跟影片做 |
| 「請預覽 / push / 開 Pages」 | 上線 |
| 「請查 game_scores 集合最高分」 | 用 Firebase MCP 直接查 |
| **「收工」** | **三方同步 + 更新工作筆記** |
| 「讀工作筆記、告訴我上次做到哪」 | 摘要進度、建議下一步 |

---

## FAQ

| 問題 | 解法 |
|------|------|
| `gh repo create` 報錯 | `gh auth login` 重登 |
| GDrive 內 `git commit` 報「cannot update the ref」 | 已設 `windows.appendAtomically false`，重試一次 |
| 重啟 Claude Code 後 `/收工` skill 沒觸發 | 確認 `~/.claude-skills/shutdown/SKILL.md` 存在；或對 Claude 說「請按 SKILL.md 收工」手動觸發 |
| 我換電腦了，怎麼接續？ | 工作檔案 GDrive 自動同步、`git pull` 拿 GitHub 最新、**重跑這份懶人包**裝 skill |
| 不想要 SessionEnd hook 了 | 編輯 `~/.claude/settings.json` 移除 `hooks.SessionEnd` 段 |
| Obsidian 工作筆記沒被自動更新 | 確認 vault 路徑寫對；對 Claude 說「請手動更新工作筆記」 |
| 我想加新工具但不確定流程 | 對 Claude 說「我想加 XXX 工具」，它會引導 |

---

## 安全與隱私

- 🔥 **`.claude/` 永遠不要 commit**（已加進 `.gitignore`）
- 學生資料**只存座號 + 班級代號**，不存真名
- API Key 不要寫死在 HTML（Firebase Config 例外，那設計可公開）
- 每次新增 Firestore 集合都要寫 Security Rules（白名單）

---

## 更新紀錄

| 日期 | 版本 | 更新內容 |
|------|------|---------|
| 2026-04-19 | v0.1 | 初版（對應 EP10 老師建專案指南影片） |

---

## 相關連結

- 📺 [影片] **Claude 基本功 EP10：老師建專案指南**
- 📖 [完整指南] `老師建專案指南.md`（GitHub: [mathruffian-dot/2026database](https://github.com/mathruffian-dot/2026database)）
- 🔧 [前置懶人包]
  - [#02 連接 GitHub](02-連接-GitHub.md)（EP05）
  - [#03 建立第二大腦 Obsidian](03-建立第二大腦-Obsidian.md)（EP07）
  - [#04.5 連接 Firebase 資料庫](04.5-連接-Firebase-資料庫.md)（EP09.5）
- 🚀 [進階] EP14 跨電腦工作術（chezmoi 多電腦同步，一般老師不需要）
